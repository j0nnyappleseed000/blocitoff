<%= form_for [Item.new], remote: true do |f| %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name, rows: 8, class: 'form-control', placeholder: "Enter new task" %>
  </div>

  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>